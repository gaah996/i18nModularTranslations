import {namespacedMessages} from '../../utils/translations';

export default namespacedMessages('LanguageSelector')({
  modalTitle: {
    id: 'modalTitle',
    message: 'Choose the new language:',
  },
});
